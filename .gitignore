# dependencies
/node_modules
/.pnp
.pnp.js

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem
Thumbs.db

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files
.env
.env*.local

# typescript
*.tsbuildinfo
next-env.d.ts

# editor
.vscode/*
!.vscode/extensions.json
.idea

# Claude Code local settings (personal permissions/paths, not shared)
.claude/settings.local.json

# tinacms — generated GraphQL client/types + the built admin UI are build
# artifacts (regenerated by `npm run tina-build` / `npm run build`), not
# source. .tina/config.ts and content/products/*.mdx are the real source.
.tina/__generated__/
public/admin/
